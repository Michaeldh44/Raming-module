# Kopieer dit naar .env.local voor lokaal testen (met `vercel dev`).
# Zet in productie deze variabele in het Vercel dashboard onder:
#   Project → Settings → Environment Variables
#
# LET OP: deel deze sleutel NOOIT, commit hem NOOIT naar GitHub.
# De .env-bestanden staan in .gitignore zodat dat niet per ongeluk gebeurt.

ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
